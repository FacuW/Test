# Sistemas-Operativos-1

# TP0 done -> cmake, conan, clang-format, cJSON, Doxyfile and github  actions for style, documentation, building and coverage check.

# TP1 done -> json metrics log generation, prometheus & grafana config for live visualization of metrics. 

# TP1 run steps (after compilation): 

1. docker-compose up -d

2. cd build/ & ./monitoreo_sistema

3. check prometheus URL: http://localhost:9090

4. http://localhost:3000, create grafana dashboard (import grafana-dashboard.json)

5. you may see the metrics graphics and stats

# TP2
# Shell
